{
  "name": "stimulize-chatroom",
  "version": "2.0.0",
  "description": "Stimulize chatroom widget for Qualtrics",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --loader:.css=text --global-name=StimulizeChatroom --outfile=dist/chatroom.min.js"
  },
  "dependencies": {
    "jquery": "3.7.1"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.0",
    "esbuild": "^0.20.0",
    "fast-check": "^3.23.2",
    "typescript": "^5.4.0"
  }
}
